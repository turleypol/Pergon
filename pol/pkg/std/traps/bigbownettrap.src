// Autot: 		Peter -pJOTTR- Straube
// kupfert by 		Fraggle
//

use os;
include "include/poison";
include "traps";

// Program Walk_on_Flame_Trap(mobile, trap)
Program Walk_on_Flame_Trap(mobile)
    if (!mobile.dead)
        syslog(mobile.name + " betritt die Falle!");
    endif
EndProgram

